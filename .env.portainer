# ========================================================
# VARIÁVEIS DE AMBIENTE PARA PORTAINER
# ========================================================
# 
# INSTRUÇÕES:
# 1. Copie este conteúdo
# 2. No Portainer, ao criar o Stack, cole na seção 
#    "Environment variables" OU use "Load variables from .env file"
# 3. Substitua os valores pelos reais
#
# ========================================================

# ===== DOMÍNIO =====
# Domínio para acesso à aplicação (ex: sefaz.seudominio.com)
DOMAIN=sefaz.seudominio.com

# ===== TRAEFIK =====
# Nome do certificate resolver do Traefik (geralmente 'le' ou 'letsencrypt')
# Verifique no docker-compose.yml do Traefik ou docker inspect traefik
CERT_RESOLVER=le

# ===== SUPABASE =====
# URL do projeto Supabase
SUPABASE_URL=https://seu-projeto.supabase.co

# Anon Key (para operações do usuário - frontend)
SUPABASE_ANON_KEY=sua-anon-key-aqui-xxxxxxxxxxxxxxxxx

# Service Role Key (para operações server-side e cron)
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui-xxxxxxxxxxxxxxxxx

# ===== SESSÃO =====
# Secret para JWT sessions (GERE UM NOVO!)
# Comando: openssl rand -base64 32
SESSION_SECRET=GERE_UM_SECRET_ALEATORIO_AQUI

# ===== AMBIENTE =====
# Ambiente de execução (production)
NODE_ENV=production

# Porta interna do container
PORT=5000

# ===== STORAGE =====
# Caminho para salvar XMLs (dentro do container)
XML_DEST_PATH=/app/xmls

# ===== PATHS NO HOST (Docker Volumes) =====
# Diretório no HOST para certificados .pfx
# Exemplo: /opt/sefaz-xml-sync/certificados
CERTIFICADOS_PATH=/opt/sefaz-xml-sync/certificados

# Diretório no HOST para XMLs baixados
# Exemplo: /opt/sefaz-xml-sync/xmls
XMLS_PATH=/opt/sefaz-xml-sync/xmls

# ===== DESENVOLVIMENTO =====
# DESABILITE EM PRODUÇÃO - permite simular respostas SEFAZ
ALLOW_SEFAZ_SIMULATION=false
